# A Node outside `engines` is refused rather than warned about, so the gate's version is the
# only one anybody installs with. pnpm's isolated tree already refuses an undeclared import.
engine-strict=true
