# A dependency that declares itself incompatible with the running Node is refused rather than
# installed. The root's own `engines` stays advisory whatever this says, measured on pnpm 12.3.4.
engine-strict=true
